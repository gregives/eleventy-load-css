{
  "name": "eleventy-load-css",
  "version": "0.0.1",
  "description": "Find dependencies in and minify CSS using eleventy-load",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "https://github.com/gregives/eleventy-load-css",
  "author": "Greg Ives",
  "license": "MIT",
  "dependencies": {
    "clean-css": "^4.2.3",
    "postcss": "^8.1.7",
    "postcss-values-parser": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "^7.16.0",
    "eslint-config-prettier": "^7.1.0",
    "eslint-plugin-prettier": "^3.3.0",
    "husky": "^4.3.6",
    "lint-staged": "^10.5.3",
    "prettier": "^2.2.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": "eslint --cache --fix"
  }
}
